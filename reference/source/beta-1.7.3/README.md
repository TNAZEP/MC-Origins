# b1.7.3 reference pair

## Purpose

Behavioral baseline. Initial reference set: both client and dedicated server. Local decompiled source and remapped jars observed on 2026-09-08; original jars were subsequently supplied and match published hashes; exact historical tool/mapping provenance remains incomplete. See the top-level manifest and inventory evidence.

## Authority

Gameplay, physics, worldgen, rendering appearance and multiplayer are AUTHORITATIVE. Architecture is the starting point to modernize. Use both sides to reconcile the a1 merge.

A newer version never overrides Beta behavior merely because it is newer.

## Provenance

Keep separate client/server originals, mappings, untouched decompiled output, optional resources, separate patches and authored notes. Record actual build labels, hashes, tools/runtime and commands in the top-level manifest. These trees are for study and are excluded from the Origins build.

## 2026-09-08 original verification

The client SHA-1 matches Mojang's version metadata; the server SHA-1 matches MCPHackers BetterJSONs for b1.7.3. Both ZIP CRC checks pass. SHA-256 and metadata sources are recorded in the manifest. No launch has been performed.

Owner reports using the most recent RetroMCP GitHub release without changes. GitHub latest was v1.2 at verification time; that is an inference about the original workflow, not verification of the exact originally used tool binary. No manual changes were reported; automatic tool patches remain unassessed. Existing source output was not regenerated or modified.
