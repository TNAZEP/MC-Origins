# 1.0a2 — Java 25, LWJGL 3 and platform boundary

**State:** Not started  
**Depends on:** 1.0a1 complete  
**Target release:** 1.0  
**Owner:** Project maintainer with Codex implementation support  
**Completion revision/date:** Not recorded

## Objective

Run both artifacts on Java 25 and replace the obsolete platform layer with LWJGL 3 without breaking a playable checkpoint.

## Entry checks

- [ ] Dependency milestone evidence is complete and CURRENT matches the repository.
- [ ] Required source/runtime/fixture inputs are present or precisely recorded as blockers.
- [ ] Current build and smoke condition is recorded before edits.

## Implementation checklist

- [ ] Pin a Java-25-compatible Gradle/plugin toolchain; replace removed APIs and launch assumptions with minimal behavioral edits.
- [ ] Introduce window/context, input/text, mouse/cursor/focus, clipboard, timing and audio services; move native calls behind their implementations.
- [ ] Port GLFW events and OpenAL/audio lifecycle, preserving Beta event order, sensitivity, sound timing and local pause semantics.
- [ ] Run an early shader/core-context spike on the proposed platforms. Provide the minimum usable shader path if fixed-function rendering prevents an a2 checkpoint.
- [ ] Remove Origins runtime reliance on LWJGL 2 and old Java; keep reference games isolated on their own runtimes.
- [ ] Qualify at least the actual development platform and record the remaining final matrix as NOT RUN.

## Exit and acceptance checks

- [ ] Both built artifacts compile and run on Java 25; local and multiplayer smoke tests pass.
- [ ] Window lifecycle, input/text, pointer lock, high-DPI coordinates, fullscreen, focus, audio and shutdown pass on the declared a2 matrix.
- [ ] No gameplay code imports GLFW; headless server uses no client native initialization.
- [ ] Baseline timing/input/behavior comparisons show no unexplained change; a usable renderer remains available.
- [ ] The final OpenGL capability target and platform flags are documented from measured context creation, with unqualified platforms visible.
- [ ] All applicable [common gates](../ACCEPTANCE.md) pass and persistent status is updated.

## Verification and evidence

Use Beta captures for behavior, both 1.13.2 sides for the LWJGL transition, and LWJGL/GLFW primary docs for native API usage. Java-port compilation alone is insufficient. If the shader bootstrap cannot remain small, extend this milestone rather than delivering an unlaunchable a2.

Record exact commands/manual steps, revision, environment, expected/observed results, evidence paths/hashes and PASS/FAIL/NOT RUN/BLOCKED in the table. No verification has been executed for this milestone yet.

| Check | Result | Evidence / reason |
|---|---|---|
| Entry baseline | NOT RUN | Implementation has not started |
| Targeted checks | NOT RUN | Implementation has not started |
| Client/server smoke | NOT RUN | Implementation has not started |
| Exit gates | NOT RUN | Implementation has not started |

## Exclusions

No modern gameplay, broad Java style rewrite, Vulkan, asynchronous simulation or integrated server.

## Recovery and checkpoint strategy

Keep platform swaps behind a narrow boundary until verified. Test failed native initialization and resource cleanup without touching existing worlds.

## Handoff and promotion

Current work: None.  
Open defects: See [issues](../status/ISSUES.md); no implementation results yet.  
Next bounded action: Verify entry checks, then take the first incomplete implementation item.  
Promotion decision: Not made. Keep this milestone uncompleted until evidence supports it. Update CURRENT, NEXT and HISTORY at every session boundary, not only promotion.
