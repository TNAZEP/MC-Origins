# 1.0a5 — Section storage with unchanged Beta bounds

**State:** Not started  
**Depends on:** 1.0a4 complete  
**Target release:** 1.0  
**Owner:** Project maintainer with Codex implementation support  
**Completion revision/date:** Not recorded

## Objective

Introduce 16-cubed sections and 0..255 storage capacity while keeping all observable Beta height and generation behavior.

## Entry checks

- [ ] Dependency milestone evidence is complete and CURRENT matches the repository.
- [ ] Required source/runtime/fixture inputs are present or precisely recorded as blockers.
- [ ] Current build and smoke condition is recorded before edits.

## Implementation checklist

- [ ] Implement sparse sections/palettes and coordinate/indexing contracts for negative X/Z and all storage boundaries.
- [ ] Separate storage capacity from Beta gameplay/generator bounds at all affected call sites.
- [ ] Adapt light, heightmaps, entity/block-entity indexing and existing persistence adapters without changing update/RNG ordering.
- [ ] Keep generator noise, sea-level threshold, population schedules, spawn ranges, placement checks and out-of-range read semantics faithful to Beta.
- [ ] Provide a temporary Beta save adapter that handles normal Beta-bounded worlds and explicitly rejects unsupported high synthetic state rather than truncating it.
- [ ] Run pre/post-population differential dumps across the seed/order corpus and a synthetic full-height storage test.

## Exit and acceptance checks

- [ ] Storage read/write/palette tests cover Y=0,15,16,127,128,255, sparse empties, packing boundaries and invalid coordinates.
- [ ] Normal generation emits unchanged Beta terrain and leaves upper storage unpopulated.
- [ ] Player placement ceiling, world-read edge semantics, lighting, spawning and random ticks match Beta.
- [ ] All controlled worldgen dumps match; no RNG or neighbor-update divergence is unexplained.
- [ ] Normal SP/MP worlds still save/reload and play; capacity tests do not enable new gameplay.
- [ ] All applicable [common gates](../ACCEPTANCE.md) pass and persistent status is updated.

## Verification and evidence

Beta is the behavior oracle; use the initial 1.13.2 and 1.17.1 references for state/section-storage study. 1.2.5 is excluded from the initial source set and should be added only if a specific historical conversion question needs it.

Record exact commands/manual steps, revision, environment, expected/observed results, evidence paths/hashes and PASS/FAIL/NOT RUN/BLOCKED in the table. No verification has been executed for this milestone yet.

| Check | Result | Evidence / reason |
|---|---|---|
| Entry baseline | NOT RUN | Implementation has not started |
| Targeted checks | NOT RUN | Implementation has not started |
| Client/server smoke | NOT RUN | Implementation has not started |
| Exit gates | NOT RUN | Implementation has not started |

## Exclusions

No higher build limit, vertical rescaling, sea-level change, negative Y support, new worldgen or parallel chunk generation.

## Recovery and checkpoint strategy

Run synthetic high-Y tests in disposable fixtures; until the new writer lands, fail explicitly if legacy output cannot represent a test world.

## Handoff and promotion

Current work: None.  
Open defects: See [issues](../status/ISSUES.md); no implementation results yet.  
Next bounded action: Verify entry checks, then take the first incomplete implementation item.  
Promotion decision: Not made. Keep this milestone uncompleted until evidence supports it. Update CURRENT, NEXT and HISTORY at every session boundary, not only promotion.
