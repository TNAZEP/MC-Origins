# 1.0a4 — Namespaced registries and real BlockStates

**State:** Not started  
**Depends on:** 1.0a3 complete  
**Target release:** 1.0  
**Owner:** Project maintainer with Codex implementation support  
**Completion revision/date:** Not recorded

## Objective

Replace foundational numeric IDs/metadata with stable identities and validated states while preserving every supported Beta behavior.

## Entry checks

- [ ] Dependency milestone evidence is complete and CURRENT matches the repository.
- [ ] Required source/runtime/fixture inputs are present or precisely recorded as blockers.
- [ ] Current build and smoke condition is recorded before edits.

## Implementation checklist

- [ ] Inventory Beta block/item IDs and meaningful metadata/durability on both sides; establish the mapping ledger.
- [ ] Implement validated namespaced keys, deterministic bootstrap/freeze and duplicate/unknown handling.
- [ ] Implement real state definitions, typed properties, legal combinations, canonical defaults and immutable BlockStates.
- [ ] Adapt simulation, inventories, rendering lookups and packets to the model without importing modern block algorithms.
- [ ] Implement explicit Beta ID/metadata adapters for the temporary existing save/network paths; isolate any legacy chunk container behind adapters.
- [ ] Inspect 1.17.1 states to draft serialization mappings and identify many-to-one cases needing later preservation metadata.

## Exit and acceptance checks

- [ ] Every supported Beta state/item is covered by a source-backed mapping and adapter round-trip or documented invalid-input policy.
- [ ] Doors, stairs, leaves, redstone, pistons, fluids and item durability retain baseline behavior.
- [ ] Duplicate identities and invalid state combinations fail clearly; names remain stable across bootstrap order changes.
- [ ] Existing save/reload and Beta packet encoding still work; no runtime index is silently written as identity.
- [ ] Client/server share one registry/model; a complete local and MP smoke test passes.
- [ ] All applicable [common gates](../ACCEPTANCE.md) pass and persistent status is updated.

## Verification and evidence

Beta sources determine meaning; both 1.13.2 sides inform registry/state and legacy-data conversion design; exact 1.17.1 source determines serialized equivalents. Test canonical states independently of their codec implementation.

Record exact commands/manual steps, revision, environment, expected/observed results, evidence paths/hashes and PASS/FAIL/NOT RUN/BLOCKED in the table. No verification has been executed for this milestone yet.

| Check | Result | Evidence / reason |
|---|---|---|
| Entry baseline | NOT RUN | Implementation has not started |
| Targeted checks | NOT RUN | Implementation has not started |
| Client/server smoke | NOT RUN | Implementation has not started |
| Exit gates | NOT RUN | Implementation has not started |

## Exclusions

No new blocks/items, modern property-driven behavior, metadata-only facade masquerading as a real state model, or general mod loader.

## Recovery and checkpoint strategy

Keep the legacy format adapter until a6 native saves pass. Unknown or unrepresentable records must fail visibly before writes, never degrade to air.

## Handoff and promotion

Current work: None.  
Open defects: See [issues](../status/ISSUES.md); no implementation results yet.  
Next bounded action: Verify entry checks, then take the first incomplete implementation item.  
Promotion decision: Not made. Keep this milestone uncompleted until evidence supports it. Update CURRENT, NEXT and HISTORY at every session boundary, not only promotion.
